# Verification plan

Before merge:

1. Typecheck/build must pass.
2. `qa-sfi-autonomous-editorial-routine.ts` must pass.
3. Cognitive/Discovery gates must remain compatible with static public surfaces and canonical publication boundaries.
4. Review must confirm there is exactly one `world_vector_reports` writer path used by the monthly routine.
5. The migration must preserve existing report classes and add only `temporal_issue_monthly` plus draft idempotency.
6. Visual cover bindings must resolve to repository-backed assets.
7. Any unrelated pre-existing program-completion debt must remain separately classified rather than masked by this PR.
