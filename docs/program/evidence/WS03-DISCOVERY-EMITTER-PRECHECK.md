# WS-03 Discovery Emitter · Precheck Evidence

Base: `ade34a5e06ad86384a4974c535d802321ee5fa65`

Existing capability inspected:
- canonical object registry and publicability gate;
- public semantic projection;
- discovery query graph, metrics and persistence;
- `sfi_external_representations` WS-03 persistence owner;
- sitemap, AI index, llms surfaces and public MCP;
- Contract Lock and WS-03 Definition of Done.

Absorb decision: one emission projection is added; no second canonical registry, identity owner, sitemap owner, MCP server or representation table is created.

External boundary: IndexNow is represented as an unavailable governed external notification until configured. This slice does not claim or fabricate external indexing receipt.
