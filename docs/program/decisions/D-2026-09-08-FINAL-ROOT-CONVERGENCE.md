# D-2026-09-08 · Final ROOT + Program Completion convergence

Status: ACCEPTED IMPLEMENTATION DIRECTIVE
Authority: founder/ROOT instruction in active SFI-00 control-room session on 2026-09-08.
Base: `ee2c999d7c5f5883d2dcac2112a06639b94bdec7`.

## Decision

The FULL REMAKE final internal convergence has two coupled repairs and MUST reuse existing canonical owners:

1. Repair `scripts/sfi-program-completion.mjs` so `SATISFIED` is reachable only from durable, requirement-bound completion evidence. Presence of code alone remains insufficient. Missing/partial/external states remain explicit. The controller must fail closed on stale, mismatched or evidence-free completion receipts.
2. Absorb the founder-selected ROOT visual system into the existing canonical `/root` surface/read contracts. Do not create a second ROOT backend, writer, authority plane or application.

## ROOT visual contract

The elected visual direction is the recovered SFI ROOT mockup family: void/charcoal field, restrained gold/amber authority accents, red only for exceptions/critical states, thin technical topology lines, institutional network/world-field visualization, compact operational typography, and a ten-module control-room composition.

Production semantics override mockup semantics:
- no simulated metrics;
- no hard-coded healthy state;
- `MISSING`, `DEGRADED`, epistemic class, source/provenance and authority state remain visible;
- ROOT remains authority + institutional work queue, not a recurring manual transition operator;
- visual lenses do not become new bounded contexts;
- all mutations keep their existing authoritative writers and ROOT gates;
- no copy may imply `MODEL OUTPUT = OBSERVATION`, `SIMULATION = OBSERVATION`, or automatic canon.

Map visual modules onto current canonical tasks/read models, including institutional state, world/observatory, evidence, cognitive spine/twin status, proposals/authority, execution/RETURN, Field/Method Lab/Studio connections, audit/logbook, and system health. Obsolete mockup labels may not resurrect deleted surfaces.

## Program-completion proof contract

A requirement may become `SATISFIED` only when the controller can reconstruct a durable receipt that is bound to the current requirement identity/text and contains non-empty evidence. Internal requirements require executable/merged/verified evidence appropriate to the requirement; external-only requirements remain `EXTERNAL_ACTION` until a real external receipt exists. A code grep is only implementation evidence and may at most support `PARTIAL` absent a valid completion receipt.

The repair MUST include a deterministic test proving:
- `SATISFIED` is reachable with a valid current receipt;
- stale/mismatched receipt does not satisfy;
- empty evidence does not satisfy;
- external action is not fabricated as satisfied;
- existing incomplete trajectory classification remains available.

## Cost/release boundary

`vercel.json -> git.deploymentEnabled=false` is already canonical from PR #442. No production Vercel deployment is used for implementation iteration. One bounded final production release is permitted only after final canonical main + required CI/assurance, when production observation is necessary for RETURN.

## Completion condition

This directive is complete only when the change is absorbed into current owners, exact-head CI/assurance passes, current ROOT authority is preserved, no simulated state is introduced, Program Completion can express evidence-backed `SATISFIED`, branch convergence is reconciled, and final production/RETURN is observed where technically possible. External account actions or a real third-party WAV upload remain explicitly external/observed requirements and may not be fabricated.