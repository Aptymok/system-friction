diff --git a/src/app/api/studio/objects/[id]/project/route.ts b/src/app/api/studio/objects/[id]/project/route.ts
index 5a9ba3f..4362b90 100644
--- a/src/app/api/studio/objects/[id]/project/route.ts
+++ b/src/app/api/studio/objects/[id]/project/route.ts
@@ -9,6 +9,7 @@ import {
 import { predictStudioFieldResponse } from '@/lib/predictive-engine/adapters/studio';
 import { getLatestPredictiveRun } from '@/lib/predictive-engine/service';
 import { createServiceSupabaseClient } from '@/runtime/supabase/server';
+import { recordObservationEvent } from '@/lib/root/telemetry/agentRegistry';
 
 export const runtime = 'nodejs';
 export const dynamic = 'force-dynamic';
@@ -81,6 +82,17 @@ export async function POST(request: Request, ctx: RouteContext) {
       if (error instanceof AmvEpistemicGateError) warnings.push(`${error.code}:${error.gate.blockers.join(',')}`);
       else warnings.push(`PREDICTIVE_ENGINE_UNAVAILABLE:${error instanceof Error ? error.message : String(error)}`);
     }
+    recordObservationEvent({
+      agentKey: 'studio_field_projection_agent',
+      signal: `Objeto ${objectId} proyectado: régimen ${projection.world.regime}, banda ${projection.fit.band}`,
+      confidence: projection.fit.confidence,
+      linked: [{ type: 'studio_object', id: objectId }],
+      evidenceUsed: projection.evidenceIds.slice(0, 8).map((eid) => ({ type: 'evidence', id: eid })),
+      patternDetected: projection.world.regime,
+      proposedAction: warnings.length ? null : projection.fit.explanation,
+      awaitingAuthorization: projection.fit.band === 'COUNTER_SIGNAL',
+    }).catch(() => undefined);
+
     return NextResponse.json({
       ok: true,
       status: projection.status,
diff --git a/src/lib/phenomena/identity/phenomenonHypothesisView.ts b/src/lib/phenomena/identity/phenomenonHypothesisView.ts
index 4a7a059..82ed699 100644
--- a/src/lib/phenomena/identity/phenomenonHypothesisView.ts
+++ b/src/lib/phenomena/identity/phenomenonHypothesisView.ts
@@ -2,6 +2,7 @@ import 'server-only';
 
 import { createServiceSupabaseClient } from '@/runtime/supabase/server';
 import { getPhenomenonState } from '@/lib/ppoi/ppoiService';
+import { recordObservationEvent } from '@/lib/root/telemetry/agentRegistry';
 
 /**
  * Phenomenon Hypothesis View — capa de LECTURA, no de escritura.
@@ -123,9 +124,25 @@ export async function getPhenomenonHypothesisView(
     items: [],
   });
 
-  return {
+  const view = {
     phenomenonId,
     relatedStudioObjectId,
     observadores,
   };
+
+  const disponibles = observadores.filter((o) => o.disponible).length;
+
+  recordObservationEvent({
+    agentKey: 'phenomenon_hypothesis_view',
+    signal: `Consolidación de ${observadores.length} observadores para fenómeno ${phenomenonId} (${disponibles} con datos)`,
+    confidence: null,
+    phenomenonId,
+    linked: observadores.map((o) => ({ type: o.fuente.toLowerCase(), id: phenomenonId })),
+    evidenceUsed: observadores.filter((o) => o.disponible).map((o) => ({ type: o.fuente.toLowerCase(), id: phenomenonId, note: `${o.items.length} item(s)` })),
+    patternDetected: disponibles < observadores.length ? 'observador_incompleto' : 'todos_los_observadores_activos',
+    proposedAction: null,
+    awaitingAuthorization: false,
+  }).catch(() => undefined);
+
+  return view;
 }
