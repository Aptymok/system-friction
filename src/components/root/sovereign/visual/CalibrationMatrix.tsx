import type { RootRow } from '@/lib/root/sovereign/rootSovereignState';
export function CalibrationMatrix({ models }: { models: RootRow[] }) { return models.length ? <div className="rs-calibration">{models.map((model, index) => <div key={String(model.id ?? index)}><strong>{String(model.name ?? model.model_key ?? 'SIN MODELO')}</strong><span>{String(model.version ?? 'SIN VERSIÓN')}</span><em>{String(model.calibration_status ?? 'NO MEDIDO')}</em><b>{typeof model.verified_sample_count === 'number' ? `${model.verified_sample_count} muestras` : 'NO MEDIDO'}</b></div>)}</div> : <div className="rs-empty"><b>SIN CONTRATO APLICADO</b><p>No hay modelos predictivos persistidos.</p></div>; }
