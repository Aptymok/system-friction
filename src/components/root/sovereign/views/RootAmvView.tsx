'use client';
import { useState } from 'react';
import type { RootSovereignState } from '@/lib/root/sovereign/rootSovereignState';
import type { RootActionRequest, RootSelection } from '../sovereignTypes';
import { AmvLattice } from '../visual/AmvLattice';

export function RootAmvView({ state, onSelect, onAction }: { state: RootSovereignState; onSelect: (selection: RootSelection) => void; onAction: (action: RootActionRequest) => void }) {
  const [query, setQuery] = useState(''); const [searchResult, setSearchResult] = useState<Record<string, unknown> | null>(null); const [searchError, setSearchError] = useState<string | null>(null); const [searching, setSearching] = useState(false); const [text, setText] = useState(''); const [source, setSource] = useState('root_manual'); const [caseId, setCaseId] = useState('');
  const [evalObjectId, setEvalObjectId] = useState('');
  async function search() { setSearching(true); setSearchError(null); try { const response = await fetch('/api/root/agentic/amv', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ operation: 'search', query }) }); const body = await response.json().catch(() => null); if (!response.ok || !body?.ok) throw new Error(body?.error ?? `HTTP ${response.status}`); setSearchResult(body); } catch (error) { setSearchError(error instanceof Error ? error.message : 'search_failed'); } finally { setSearching(false); } }
  return <section className="rs-view"><div className="rs-view-title"><span>AMV</span><h1>PERSISTENT SIGNAL LATTICE</h1><p>Memoria real, recurrencia y contradicción; ingest no equivale a verdad verificada.</p></div><div className="rs-amv-grid"><article className="wide"><header>MEMORY LATTICE</header><AmvLattice memories={state.amv.data.memories} onSelect={onSelect} /></article><article><header>SEARCH MEMORY</header><form className="rs-form" onSubmit={(event) => { event.preventDefault(); void search(); }}><label>QUERY<input value={query} onChange={(event) => setQuery(event.target.value)} /></label><button type="submit" disabled={searching}>{searching ? 'SEARCHING' : 'SEARCH'}</button></form>{searchError ? <div className="rs-source-warning">{searchError}</div> : null}{searchResult ? <pre className="rs-result">{JSON.stringify(searchResult, null, 2)}</pre> : null}</article><article><header>INGEST AMV</header><form className="rs-form" onSubmit={(event) => { event.preventDefault(); if (!text.trim()) return; onAction({ id: 'amv-ingest', label: 'INGEST AMV', effect: 'Registra memoria declarada. No la convierte en evidencia verificada.', target: caseId || source, endpoint: '/api/root/agentic/amv', method: 'POST', body: { operation: 'ingest', text, source, caseId: caseId || null } }); }}><label>SOURCE<input value={source} onChange={(event) => setSource(event.target.value)} required /></label><label>CASE ID<input value={caseId} onChange={(event) => setCaseId(event.target.value)} /></label><label>TEXT<textarea value={text} onChange={(event) => setText(event.target.value)} required /></label><button type="submit" disabled={text.trim().length < 6}>REVIEW INGEST</button></form></article><article><header>MIHM EVALUATE (FASE 1)</header><p className="rs-view-note">Corre generatePhase1Report sobre las variables MIHM reales ya derivadas del objeto en /studio. No recalcula nada — traduce y persiste.</p><form className="rs-form" onSubmit={(event) => { event.preventDefault(); if (!evalObjectId.trim()) return; onAction({ id: `mihm-evaluate-${Date.now()}`, label: 'MIHM EVALUATE', effect: `Corre Fase 1 sobre el objeto ${evalObjectId} y persiste el reporte en amv_predictions.`, target: evalObjectId, endpoint: '/api/root/evaluate', method: 'POST', body: { objectId: evalObjectId } }); }}><label>STUDIO OBJECT ID<input value={evalObjectId} onChange={(event) => setEvalObjectId(event.target.value)} required /></label><button type="submit" disabled={!evalObjectId.trim()}>PREPARAR EVALUACIÓN</button></form></article><article><header>ATTRACTORS / CONTRADICTIONS</header><div className="rs-card-list">{state.amv.data.attractors.slice(0, 8).map((item) => <button type="button" key={String(item.id)} onClick={() => onSelect({ kind: 'attractor', id: String(item.attractor_key ?? item.id), title: String(item.label ?? 'Attractor'), source: 'sfi_attractors', observedAt: typeof item.updated_at === 'string' ? item.updated_at : null, confidence: typeof item.confidence === 'number' ? item.confidence : null, evidenceIds: [], warning: null, data: item })}><span>{String(item.status ?? 'SIN DATO')}</span><strong>{String(item.label ?? item.attractor_key)}</strong><em>EVIDENCE {String(item.evidence_count ?? 'NO MEDIDO')}</em></button>)}{state.amv.data.ejectors.slice(0, 8).map((item) => <button type="button" key={String(item.id)} onClick={() => onSelect({ kind: 'contradiction', id: String(item.ejector_key ?? item.id), title: String(item.label ?? 'Contradiction'), source: 'sfi_ejectors', observedAt: typeof item.updated_at === 'string' ? item.updated_at : null, confidence: null, evidenceIds: [], warning: null, data: item })}><span>CONTRADICTION</span><strong>{String(item.label ?? item.ejector_key)}</strong><em>{String(item.status ?? 'SIN DATO')}</em></button>)}</div></article></div></section>;
}
