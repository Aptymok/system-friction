import { NextResponse } from 'next/server';
export const dynamic='force-dynamic';
export async function GET(){return NextResponse.json({ok:true,name:'SFI External Agent Gateway',version:'1.1',auth:'Bearer token',base:'/api/external/v1',operations:[{id:'observe',method:'POST',path:'/observe',scope:'observe',description:'Read governed SFI state through an allowlisted surface.'},{id:'propose',method:'POST',path:'/propose',scope:'propose',description:'Submit a governed action proposal. ROOT approval remains mandatory.'},{id:'execute',method:'POST',path:'/execute',scope:'execute',description:'Realize an already queued proposal inside SFI. It cannot approve its own proposal or perform an ungoverned external action.'}],governance:'External agents may observe, propose and realize already-authorized internal actions. Approval remains a distinct ROOT decision and all realization preserves lineage.'});}
