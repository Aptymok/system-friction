---
title: "Sistemas que no pueden permitirse fallar"
doc_id: "core-bridge"
series: "core-bridge · Puente"
summary: "Umbral real vs oficial. La distancia donde opera el operador."
version: "1.0"
node: "docs"
patterns:
  - umbral-real
  - distancia-umbrales
  - fricción-política
---

## El umbral que no se nombra

Todo sistema crítico opera con dos umbrales simultáneos: el que se declara y el que importa.

El umbral oficial define cuándo el sistema admite que algo falló.
El umbral real define cuándo el sistema deja de funcionar para quienes dependen de él.

La distancia entre ambos no es error de medición. Es donde ocurre la operación real.

## Por qué existe esa distancia

Admitir el umbral real implica responsabilidades que el sistema no está diseñado para asumir.
Entonces el sistema no lo admite. Calibra sus indicadores para mantener la distancia invisible.

No por mala fe. Porque hacer visible esa distancia costaría más que mantenerla opaca.

## Dónde opera quien lee esto

En esa distancia.

No en el sistema declarado ni en el umbral que nadie nombra.
En el espacio entre ambos, donde las decisiones reales se toman sin el respaldo que el sistema supone que existe.

## Por qué esto es un puente

Este documento conecta los marcos abstractos del ecosistema con los casos específicos de los nodos.

No es un documento de patrón. Es una condición de lectura.
Quien llega aquí ya sabe que la distancia existe.
Lo que viene después son sus instancias verificables.
