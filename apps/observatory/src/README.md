# apps/observatory/src

Scaffold read-only del dashboard constitucional del Observatorio SFI.

Esta carpeta no contiene UI productiva en FASE 5B.

Solo declara contratos de vista para que una futura implementacion pueda consumir:

- `FieldStateDTO`
- `NodeStateDTO`
- `LogEntryDTO`
- `SourceHealthDTO`

Regla central:

La UI no calcula verdad. La UI observa.

